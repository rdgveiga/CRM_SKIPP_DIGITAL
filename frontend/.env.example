# Frontend - use somente para o Realtime (URL e ANON key são seguras para expor).
# Preencha e salve como frontend/.env. No Vercel, configure as mesmas variáveis.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=